# Workflow audit records

- `tasks/` contains records generated by `scripts/agent_workflow.py`.
- `bootstrap/` contains explicitly marked migration records for calls that
  happened before the state machine was installed.

These records contain no API keys. Accepted task records should be committed
with the implementation they validate.
