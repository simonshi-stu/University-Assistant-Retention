# 高校课程注册与学习留存分析平台｜项目范围与执行协议

版本：2026-09-10

## 时间范围与归属边界

- 分析参考时点设为 2024-01-01；仓库实际开发仍从 2026-09-10 开始，二者必须分开记录。
- 主窗口：2021、2022、2023。只要满足当前分析模块的数据充足条件，就不得升级到后续年份。
- 第一回退：2022、2023、2024。仅用于主窗口不足的模块。
- 最后回退：2023、2024、2025。仅用于第一回退仍不足的模块。
- 不使用 2026 年及之后的数据。
- 模型按窗口前两年训练、第三年留出验证：2021–2022→2023；2022–2023→2024；2023–2024→2025。
- 数据窗口按模块确定；排课、W proxy、情感分析和模型不必强制使用同一回退层级。
- 2024 或 2025 数据产生的结果只能作为后续独立延伸，不得归入 2024 年 ATLAS 已完成成果。

本次用户明确批准的 `extended_2024_backtest` 是不替代主线的独立六年
`course_trajectory` 扩展：仅使用 2019–2024 年 Spring/Fall 原始 GPA 数据。纳入普通
课程的稳定键为 `Subject + Number`；`Course Title` 仅作展示与审计，标题变化不拆分
`course_variant`。可识别的 `Special/Selected Topics` 按原始行单独审计，并从普通轨迹与
模型输入中排除；仅含专题行的键排除，与普通行重叠的键保留普通行。2021–2023 是重点
窗口；对 2024 桥接/真值留出，使用具有 2021、2022、2023 全三年记录的课程。历史
`lag` 与资格规则以当前 `course_trajectory` 源代码和 README 为准：普通课程按前一
日历年的任一 Spring/Fall 观察取历史，真正只在一个季节出现的课程才按同季取历史，
目标行须具备完整的三年前置年份历史。该扩展独立于 2021–2023 主分析，不回写 ATLAS。

## 一、项目目标

构建一个可复现的高校课程分析平台，完成以下闭环：

`public data → Python ingestion → split/clean/validate → normalized tables → metrics/models → local website/Excel output`

项目重点不是制作静态图表，而是展示：

- 数据粒度和字段含义理解；
- 数据清洗、质量检查和多源身份映射；
- 课程需求、成绩分布和退出代理分析；
- 课程特征与 W-based withdrawal proxy 的关联建模；
- 评论情感和主题分析；
- 课程时间冲突与编排支持；
- 可复现的本地交付和清晰的限制说明。

## 二、推荐数据源

### UIUC 课程规划数据

- Course Explorer API：课程、学期、课程安排、先修关系和 section 信息。
- UIUC GPA Dataset：成绩分布、课程人数、W 数量和教师信息。
- UIUC 公开招生报告：学院、专业和学生层级的聚合招生信息。
- ICES：只有在用户拥有合法校园访问权限并获得允许的情况下使用；不绕过登录、不抓取受限制内容。
- 默认只使用 2021–2023；不足时依次尝试 2022–2024 和 2023–2025，并单独记录每个模块的选择理由。

### 暂不纳入主分析的数据

- OULAD 主要课程数据不在 2021–2025 目标窗口内，因此从主模型和主看板中撤下。
- 没有合法、同口径的学生考勤数据时，不生成或模拟 UIUC attendance 字段。
- 若以后使用 OULAD，只能作为隔离的历史方法演示，不得与 UIUC 2021–2025 指标合并，也不得进入本项目的核心结论。

### 模块级数据充足条件

主窗口满足以下最低条件时不得回退：

- 三个目标年份均有记录，且每年至少包含两个主要学期中的一个；
- W proxy 分析至少有 200 条分母有效的 course-section-term 记录；
- 高 W 风险分类在训练窗口至少有 20 个正类和 20 个负类样本；
- 排课分析至少有 100 条带有效 meeting day、start time、end time 的 section 记录；
- 情感分析至少有 200 条合法取得、带课程身份和时间标签的评论。

某一模块不满足条件时，只为该模块启用下一窗口，并把缺失覆盖、过滤前后行数及回退理由写入 `HISTORY.md`。

## 三、核心分析问题

### 课程需求与退出

- 哪些课程在不同学期持续高需求？
- 哪些课程的 W-based withdrawal proxy 较高？
- 课程等级、成绩分布、先修要求、教师和课程时间是否与退出代理相关？

### 课程级风险建模

- 使用前两年的历史课程特征，能否识别第三年高 W 风险课程？
- 历史 W proxy、平均 GPA、课程级别、学期、授课教师历史和时间段中，哪些变量与风险关联最稳定？
- Logistic Regression 与 Random Forest 的结果是否一致，复杂模型是否带来足够改善？

### 评论与课程体验

- 负面评论是否集中在 workload、exam、assignment、grading 或 instructor？
- 评论情绪与课程成绩分布或 W-based proxy 是否相关？
- 结论是否受评论自选择偏差影响？

### 课程编排

- 哪些课程组合冲突最严重？
- 哪些时间段承载了过多高需求课程？
- 哪些院系的核心课冲突集中？
- 是否存在某些课程只能安排在低需求时间段？
- 课程安排变化是否与后续课程选择人数变化相关？

以上五个问题是最终看板和验收报告的必答问题。若数据无法支持某个问题，必须明确写出缺少的字段、可回答到什么程度以及不能得出的结论，不能用空图或泛化文字代替。

## 四、模型策略

### 首选监督学习

目标是预测第三年课程是否进入高 W 风险组。高风险阈值只使用训练年份计算，默认采用同课程层级或院系内训练样本的上四分位数，避免使用验证年份信息定义标签。

先使用 Logistic Regression 作为主模型，再使用 Random Forest 作为非线性对照。

- Logistic Regression：适合二分类，系数方向可解释，能够说明历史 W proxy、课程等级、时间段等变量与高风险的关系；数据量中等时稳定、训练快，最符合本项目“解释课程问题”的目标。
- Random Forest：用于检查非线性和变量交互，例如课程等级与时间段共同影响风险；不需要深度模型规模的数据，但解释性弱于 Logistic Regression，因此只作为对照。
- 不优先使用神经网络、XGBoost 或复杂时间序列模型：本项目样本单位是 course-section-term，样本规模和连续时间长度有限，主要目标是可解释分析而非追求极小的预测增益。
- 如果 Random Forest 没有明显改善 PR-AUC、Recall@K 或错误分布，则保留 Logistic Regression 作为最终模型。

### 非监督学习

使用 K-Means 对课程进行画像，聚类只能描述相似课程组，不能直接证明退课原因。

可聚类的对象包括：

- 课程画像：需求代理、平均 GPA、W proxy、D/F/W 比例、课程等级和开课频次；
- 时间段画像：课程数量、高需求课程数量和冲突总权重；
- 评论主题：使用 TF-IDF 后的文本特征进行聚类，仅在评论量满足最低条件时启用。

选择 K-Means 的原因是实现简单、运行快、结果可通过聚类中心解释，并且适合数值型标准化特征。若轮廓系数低或聚类不稳定，就不强行给课程贴标签，只保留描述性分组。

### 评估原则

- 固定使用前两年训练、第三年验证，不做随机拆分。
- 关注 PR-AUC、Recall@K、F1 和 calibration，而不是只看 accuracy。
- 对类别不平衡使用 class weight、阈值分析或 PR 曲线，不直接过采样造成数据泄漏。
- 记录每次实验的数据版本、特征窗口、模型参数和结果。

## 五、交付物

### 数据层

- `data_dictionary.md`
- 原始数据来源和许可证记录
- 标准化课程表、section 安排表、成绩表、课程评价表
- 数据质量报告

### 分析层

- 课程需求和 W-based proxy 分析
- 课程级高 W 风险 Logistic Regression 与 Random Forest 对照
- K-Means 课程画像（仅在聚类质量可解释时保留）
- 评论情感和主题分析
- 排课冲突图、时间段负载与课程安排变化分析
- 模型评估和误差分析报告

### 展示层

- 本地 Streamlit website，或同等本地网页应用
- Excel workbook，包含 Raw、Cleaned、Metrics、Model Evaluation、Schedule Conflicts 和 Summary 等工作表

### 看板必答输出

看板和最终报告必须使用生成表格中的数值直接回答：

1. 冲突权重最高的课程组合及其共同时间段；
2. 高需求课程最拥挤的时间段；
3. 核心课冲突最集中的院系；
4. 可行时间槽有限、只能进入低需求时间段的课程；
5. 同一课程改变时间安排后，后续学生数或需求代理的变化。

每项回答必须能追溯到明细表和计算字段。第五项只能表述为观察到的关联，除非后续设计能够支持因果结论。

## 六、精简验证与监督验收

不建设过度保护性的测试体系。每个阶段只保留以下必要检查：

- 年份窗口、原始行数、过滤行数和最终行数；
- 字段含义、数据类型、缺失值、重复值和 join cardinality；
- W proxy、需求代理、冲突权重、模型指标和图表汇总值复算；
- Python 数据管线能够从头运行一次；
- 本地看板能够启动并加载主要页面一次；
- 图表标题、轴、单位、颜色、筛选器和文字结论与底层表格一致；
- 五个排课问题均有数据支持的回答或明确的数据不足说明。

DeepSeek V4 Flash 产出代码和初步分析；Codex 逐阶段检查代码、表格、结果与看板，给出接受、退回修改或证据不足结论。Codex 不承担主功能代码编写，也不建立自动任务调度。

## 七、禁止事项

- 不将新项目内容写回 ATLAS 实习经历。
- 不把 UIUC 的 W 数量写成完整注册过程中的真实 drop rate。
- 没有合法考勤字段时，不生成 attendance 结论。
- 不声称评论分析证明了课程难度导致退课。
- 不使用受限个人学生数据、NetID、姓名或邮箱。
- 不绕过校园登录、不绕过网站访问限制、不抓取违反服务条款的数据。
- 不使用 Tableau。
- 不把 JSON/LLM 输出未经验证地当成事实。
- 不修改本仓库以外的任何仓库或文件。
- 不使用 2026 年及之后的数据。
- 不让 OULAD 的旧年份数据混入 2021–2025 主看板。

## 八、历史留痕

`HISTORY.md` 是追加式记录，必须持续维护：

- 当前阶段、完成比例和下一验收点；
- DeepSeek 使用的输入目标和返回产物；
- Codex 检查了哪些代码、表格、模型结果和图像；
- 接受、退回或证据不足的原因；
- 数据窗口选择、回退理由和实际使用年份；
- 已发现问题、修复结果和仍存在的限制；
- 可支持与不可支持的简历措辞。

## 九、简历使用边界

在代码、数据、实验记录和本地网站真正完成以前，只能写项目计划，不能写完成结果。

完成后建议以独立项目呈现：

`高校课程注册与学习留存分析平台｜Python、Pandas、scikit-learn、Streamlit、Excel`

项目开发日期应填写真实日期。若项目实际从 2026-09 开始，不能将其写成 2024 年 ATLAS 实习期间的成果。
